# Application
DEBUG=false
APP_NAME=LeadMachine
APP_VERSION=0.1.0

# Database
DATABASE_URL=postgresql+asyncpg://leadmachine:password@db:5432/leadmachine
POSTGRES_USER=leadmachine
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=leadmachine

# Redis
REDIS_URL=redis://redis:6379/0

# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key

# SMTP (docker-mailserver)
SMTP_HOST=mailserver
SMTP_PORT=25
SMTP_USER=noreply@lm.allardvolker.nl
SMTP_PASSWORD=change-me-in-production
SMTP_FROM_EMAIL=noreply@lm.allardvolker.nl

# IMAP (docker-mailserver)
IMAP_HOST=mailserver
IMAP_PORT=993
IMAP_USER=inbox@lm.allardvolker.nl
IMAP_PASSWORD=change-me-in-production

# JWT Authentication
JWT_SECRET=change-this-to-a-secure-random-string-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Tracking
TRACKING_BASE_URL=https://lm.allardvolker.nl

# Rate Limiting
EMAIL_DAILY_LIMIT=50
EMAIL_MIN_DELAY_SECONDS=120
EMAIL_MAX_DELAY_SECONDS=300

# Scoring thresholds
SCORE_HOT_THRESHOLD=75
SCORE_WARM_THRESHOLD=60
SCORE_COOL_THRESHOLD=45
